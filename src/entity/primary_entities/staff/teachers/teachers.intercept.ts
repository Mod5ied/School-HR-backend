/* Check if each cahe requests already exists and return else, proceed to the middlewares/services. */
// have an 'cache-return' interceptor for at most each get request.

/* E.g: the account fetch route must have a validator to ensure that phone-numbers 
    are greater than or equal to 11-digits.
*/